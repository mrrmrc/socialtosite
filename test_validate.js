const fs = require('fs'); const urls = ['http://localhost/example', 'http://127.0.0.1/example', 'http://192.168.1.10/example', 'http://169.254.169.254/latest/meta-data', 'https://user:password@example.com/', 'https://example.com:8443/', 'file:///etc/passwd']; urls.forEach(u => { try { const parsed = new URL(u); console.log('Parsed:', u); } catch(e) { console.error('Failed:', u); } });
